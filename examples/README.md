# Example state

These files show the JSON records written by the prototype after a few sample requests. The application does not load them at runtime.

New runs start with empty stores under `.data/`. Set `PROVENANCE_DATA_DIR` before starting the application to use another local directory.
